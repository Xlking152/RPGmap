# 更新日志

本文件记录北宋兰州 RPG 矢量地图的版本演进，内容由原工程留痕整理而来，并按“最新版本优先”排列。

当前应用版本：`1.1.0`；地图数据版本：`1.0.5`；存档格式：`SaveV2`。应用版本与地图数据版本相互独立；升级地图数据版本时，需要同步维护兼容版本或迁移逻辑。

## 1.1.0

- 地图数据升至 `1.0.5`，55 栋建筑加入用途、构造、说明、可进入状态和稳定入口；州衙仪门作为通道保持不可进入，其余 54 栋可进入。
- 攻击范围加入独立弹坑开关，提交时冻结 `craterPolygon`；临河 12 米内可生成 6 米入水沟，完整建筑和城墙阻断连接，接触弹坑按 1 米容差传播。
- 新增 `deriveFloodRegions()`，输出水源、区域类型、事件和多边形；原 `deriveFloodPolygons()` 继续提供兼容结果。
- 新增独立角色系统与 SaveV2 `characters`，支持姓名首字兜底、128×128 WebP 圆形头像、角色锚点、进入/离开建筑和建筑整毁疏散。
- 新增 `navigation.js`，使用打包内联的 EasyStar 在 10 米网格执行 A*；道路代价 1.0、普通地面 1.7，动态障碍随破坏、恢复、撤销、重置和导入重建。
- 角色移动采用预览、确认和动画流程；确认后立即保存终点，动画限制为 0.8–6 秒，刷新不会留下半途状态。
- 自动化测试增加到 67 项；桌面和 390px 浏览器验收覆盖临河弹坑、角色路线、头像、建筑详情、内部角色和整毁疏散，控制台 0 错误，正文无横向溢出。
- 最终单页保持 4.2 MB 上限内，应用版本 `1.1.0`、地图版本 `1.0.5` 和 SaveV2 已写入工程文档与运行包校验记录。

## 1.0.13

- 重新检查 18 张普通建筑素材的有效 Alpha、主体包围盒、地图足迹比例和明暗背景边缘，淘汰伪棋盘格、无 Alpha、彩色边缘和主体比例不合格的候选图。
- 使用内置 ImageGen 全量重生成州衙仪门/廊房、营房、仓廪、马厩、工坊、市肆、普通民居和院落民居，统一为约 80 度近正交高俯视、屋顶主导、入口朝下的淡彩木刻质感。
- 18 张成品统一裁切、保留约 3% 安全边距并按各类足迹比例透明补边，禁止非等比拉伸；均为 `yuva420p`、最长边 768px，总量 `1,137,000` 字节，单张最大 `84,708` 字节。
- 生成建筑渲染改为 `preserveAspectRatio="xMidYMid meet"`；普通建筑 SVG 底衬降至 0.24，台基和阴影减重，并隐藏与位图重复的瓦线、门、棚布、马厩隔栏和工坊设施。
- 州衙与市易务补充克制的台基层级；建筑局部破坏改为基于稳定种子的 14 点深色不规则缺口，并在足迹内增加梁木与瓦片碎片，消除规则圆斑观感。
- 自动化测试增加到 61 项；地图坐标、足迹、Feature ID、命中几何、地图数据版本 `1.0.4` 和 SaveV1 均保持不变。

## 1.0.12

- 使用内置 ImageGen 生成 18 个普通建筑透明变体，覆盖州衙仪门与廊房、营房、仓廪、马厩、工坊、市肆、普通民居和院落民居共 50 个 Feature；存在伪棋盘格的参考图批次全部废弃并重新生成真 Alpha 版本。
- 18 张素材统一裁至约 3% 安全边距、最长边 768px，并输出带 Alpha 的 WebP；总量 `1,262,616` 字节，单张最大 `90,728` 字节。
- `artAssets` 增加冻结的 `buildingSprites` 九组数组；渲染按 `category + renderType` 选组，用 Feature ID 的 FNV-1a 稳定分配变体。默认地图包仍为纯 SVG。
- 50 张实例位于各自 `data-feature-id` 图组内，生成图层透明度为 0.62，SVG 屋体底衬为 0.42，台基和阴影保持完整；既有州衙与市易务地标统一使用相同混合层级。
- 不修改坐标、足迹、角度、Feature ID、碰撞、地图数据版本或 SaveV1；九类原型均通过局部破坏、整毁、恢复和撤销恢复测试。
- 自动化测试增加到 59 项；真实 Chromium `file://` 验证 50 栋、18 变体、桌面/390px 无横向溢出、控制台 0 错误、外部请求 0，最终单页保持在 4.2MB 上限内。

## 1.0.11

- 新增 `map-presentation.js`，按 SVG 屏幕缩放比例切换总览、中景和近景；普通建筑、院落细节、道路小巷和标签随层级渐进呈现。
- 标签增加优先级、最小/最大缩放档、障碍物和候选位移元数据；运行时使用屏幕空间包围盒避让其他标签、核心地标和视口边界。
- 生成式地标统一降低饱和与对比、提高明度，并恢复低透明度 SVG 底衬，减弱位图与矢量建筑之间的黑度和细节落差。
- 整毁建筑增加稳定分布的梁木与瓦片；浮桥整毁改为两端残桥、断口、漂散舟体和浮木；城墙局部破坏增加锯齿深色缺口与散石。
- 新增“检查地物”工具，使用地图坐标和 Feature 多边形命中建筑、城墙、城门、植被和浮桥；重叠对象按视觉重要度和足迹面积排序。
- 右侧地物面板显示公开名称、类别、对象 ID、完整/局部破坏/整毁状态和中心坐标，并通过既有 `restore` 场景事件精确恢复所选对象。
- 场景记录增加去重后的命中数量与类别摘要，点击记录可定位并高亮关联对象；破坏预览增加可点击的具体目标列表。选择与高亮保持运行时状态，不进入 SaveV1。
- 自动化测试增加到 57 项；真实 Chromium 验证三档可见标签均无重叠，五类典型地物可点击选中，恢复后撤销可重新显示破坏，390px 页面无横向溢出，控制台 0 错误且离线外部请求为 0。

## 1.0.10

- 使用内置 ImageGen 重新生成黄河浮桥，改为窄桥面、低矮浮舟、短船艏和不规则绑扎，消除旧素材规则梳齿状轮廓。
- 新原图自带有效透明通道，清理 7 个游离像素后裁切并压缩为 `159 × 1024`、约 65 KB 的透明 WebP，未进行二次色键以避免损伤木绳边缘。
- 地图渲染宽度从 210 收到 132 世界单位，两端继续对准原道路，SVG 回退、Feature ID、碰撞足迹、破坏逻辑和地图数据版本均保持不变。
- 自动化测试维持 47 项；总览、近景、破坏态与浏览器控制台检查通过。

## 1.0.9

- 使用内置 ImageGen 生成黄河浮桥、州城角楼、市易务正厅和市易务货栈四项俯视素材，并按统一色键、边缘收缩、主体裁切和 WebP 压缩流程处理。
- 四座州城角楼新增为独立 `wall-tower` Feature，地图数据版本升至 `1.0.4`；旧版 `1.0.0` 至 `1.0.3` SaveV1 均可自动迁移。
- 浮桥位图按原中心线旋转，生产美术包隐藏其 SVG 桥体底衬；默认纯矢量地图包仍完整保留原桥体。
- 市易务正厅与货栈分别保留独立建筑足迹和图组，七个新增实例均随对应对象参与破坏、撤销与重放。
- 自动化测试增加到 47 项，并验证四座角楼 ID、墙体类别、建筑零重叠、资产隔离和 Feature 归属。

## 1.0.8

- 使用内置 ImageGen 生成州城门楼和金城关关楼俯视素材，经纯绿色色键、边缘收缩、主体清理和 WebP 压缩后作为本地资产接入。
- 四座州城门复用同一门楼皮肤并按原足迹旋转，金城关使用独立的厚重关楼皮肤；五个实例均保留中轴通行孔。
- 门楼位图分别位于 `gate-north`、`gate-east`、`gate-south`、`gate-west` 与 `jincheng-gatehouse` Feature 内，跟随原对象参与局部破坏、整毁、撤销和重放。
- 默认地图包继续保持纯矢量，生产入口通过 `cityGatehouseUrl` 与 `jinchengGatehouseUrl` 显式注入两项资产。
- 自动化测试维持 46 项，并扩展地图包契约，锁定四座州城门和一座关楼的实例数量、归属图组与默认包隔离。

## 1.0.7

- 使用内置 ImageGen 生成州衙大殿和州城隍神祠俯视素材，以纯绿色背景生成，再经官方色键工具、1px 边缘收缩和最大连续主体清理输出透明 WebP。
- 州衙正堂与后堂分别在自身 Feature 内渲染同一大殿皮肤；仪门和东西廊房继续使用 SVG，保持五个州衙对象独立破坏。
- 神祠整院落素材位于 `chenghuang-temple-compound` 图组内，南门、正殿、侧廊、香炉与现有足迹语义一致。
- 默认地图包继续保持纯矢量；生产入口通过 `chenghuangTempleUrl` 与 `yamenHallUrl` 显式注入两项地标资产。
- 自动化测试增加到 46 项，锁定默认包隔离、地标数量，以及位图必须位于对应可破坏 Feature 图组内。

## 1.0.6

- 顶部工具栏移除平台相关的 Emoji 字形，改用按需打包并内联的 Lucide 线性图标；应用标题继续使用安全的 `textContent` 写入。
- 界面色彩改为砖红执行色、河青选择色与冷灰中性色，减少大面积米黄色对操作层级的干扰。
- 右侧面板移除嵌套卡片外观，改为连续分隔段；侧栏由 370px 收到 350px，增加桌面地图可用面积。
- 标准 1366px 桌面工具栏保持单行；768px 和 390px 宽度改为可横向滚动的工具带，地图与面板上下排列。
- 修复移动断点只声明 `min-height` 导致 Leaflet 地图无法填满容器的问题，并增加图标、配色与无渐变界面契约测试。
- 自动化测试增加到 45 项；桌面、窄屏和手机宽度浏览器检查均无控制台错误、正文横向溢出或按钮文字溢出。

## 1.0.5

- 使用内置 ImageGen 生成低对比黄土地表、黄河流纹和六变体建筑瓦砾图集，统一为宋代矿物淡彩与轻木刻质感。
- 生成式资产压缩为本地 WebP，并由 Vite 内联到单页；正式成品仍无外部资源请求。
- 地图包增加可选 `artAssets` 注入；默认地图包继续保持纯矢量，生产入口显式启用美术资产。
- 整毁建筑按 Feature ID 稳定选择瓦砾变体，贴花缩入并裁切在碰撞足迹内；刷新、撤销和重放结果一致。
- 自动化测试增加到 43 项，增加生成式资产隔离与地图包工厂契约测试。

## 1.0.4

- 为地图数据版本增加 `compatibleMapVersions` 和 SaveV1 自动迁移。
- 浏览器自动存档迁移前保存原始备份；备份或写入失败时暂停自动保存。
- 为破坏预览增加几何与目标类别签名，参数变化后立即失效，提交时再次计算并校验。
- 导入文件增加 5 MB 上限，并限制标记、攻击范围、场景事件、引用与多边形点数。
- 动态应用标题改用 `textContent` 写入，避免通用地图包标题被解释成 HTML。
- 将浏览器存档读写拆为 `src/app/storage.js`，集中管理迁移备份、延迟写入和写入失败熔断。
- 将场景 SVG 派生效果拆为 `src/render/scene-renderer.js`，隔离废墟、裁切损伤、弹坑和倒灌渲染。
- 自动化测试增加到 42 项，补齐持久化迁移、无效存档备份、写入熔断与延迟保存覆盖，并同步发布哈希。

## 1.0.3

- 将 SVG 重组为基底、液体、可破坏、破坏、漫灌和标签层。
- 建筑、树木和桥梁改为 95% 覆盖才整毁，低覆盖只产生局部破坏；墙体和地表始终局部裁切。
- 增加严重破坏弹坑、黄河连通判定和液体倒灌视觉。
- 成品移除史料开关与 A/B/C 覆盖标注，历史资料继续保留在工程目录。

## 1.0.2

- 修复 Edge 与内置浏览器显示不一致：旧逻辑会把 Edge 本地保存的“合理推定=false”应用到全局 `data-confidence="B"`，从而只隐藏城隍庙本体而保留标签。
- 史料开关现仅控制显式史料覆盖层和 `#layer-evidence` 内的 A/B/C 标注。
- 城隍庙增加 `data-landmark-visibility="always"`，作为标志性建筑始终显示；场景破坏仍可正常隐藏庙体与关联标签。
- 加强城隍庙红褐色院墙、屋顶和标签描边，使其在总览缩放下也容易识别。
- 工作区整理为独立“工程留痕”和“最终运行包”，不再长期保存重复成品 HTML 或压缩包。

## 1.0.1

- 增加城隍庙独立院落，移除原位置低可信度店铺，保持建筑和道路碰撞为零。
- 增加框选标记、`Delete` 批量删除和清空全部标记。
- 将场景历史改为线性记录：撤销直接删除最后一条，彻底重置直接清空全部破坏/恢复记录。
- 兼容导入旧版 `reset/undo` 追加式记录，并自动规范化为 `damage/restore` 线性历史。

## 1.0.0

- 将旧 Canvas/PNG 思路重构为离线 SVG 单页地图。
- 建立通用标记、两点测距、路线测距、攻击范围、SaveV1 和场景破坏接口。
- 建立建筑/道路缓冲碰撞测试和对象式/裁切式破坏规则。
