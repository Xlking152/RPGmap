# RPGmap v2.5.2

本版本包含大范围视野优化及框架审计后的修复。

- 大范围视野和沿途探索使用后台 Worker，地面遮挡按行计算建筑阴影，减少地图交互等待。
- 修复并发探索区域漏记、默认场景探索被丢弃、替换 World 后旧探索结果回写的问题。
- 修复 Worker 异常处理与销毁生命周期，主线程和 Worker 共用视野计算，保持明暗、精确与模糊视野一致。
- 修复关闭 LOS 时照明网格仍遮挡视野的问题；状态更新复用共享角色解析，保留实例隔离。
- 删除未使用的状态徽章模块，补充框架审计和回归测试。

下载 `RPGmap-v2.5.2.zip`，解压后双击 `start-rpgmap.bat`。升级前备份旧目录中的整个 `map/`，再迁移到新包。存档与通信 schema 不变；主机和玩家请统一升级至 v2.5.2。

大范围场景仍可能出现帧停顿，本版本不承诺所有地图和设备均稳定 60 FPS。
