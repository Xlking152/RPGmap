# 分段移动距离与计价档位

- waypoint 移动路径现在会在每个分段中点显示独立距离标签，终点同时显示整条路线的总计价距离。
- 新增 `5 / 10 / 20 / 50 / 100 m` 五档移动距离显示/计价单位，默认 `5 m`。
- 每段计价值按所选档位向上取整后累加；真实 A* 路径长度、寻路、碰撞和最终落点保持不变。
- 档位作为本地 UI 偏好保存，不写入 SaveV2，也不会影响地图数据兼容性。
- 已处理 waypoint 与当前终点重合的边界情况，避免产生额外的零长度分段标签。
- 完整自动化测试现为 `75/75` 通过。
