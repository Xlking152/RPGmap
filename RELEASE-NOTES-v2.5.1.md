# RPGmap v2.5.1

- 修复单机几百米至千米视觉范围下放置 Token 延迟、长距离移动卡住的问题。实时遮罩与沿途探索移至后台 Worker，地图交互无需等待探索完成。
- 地面视野使用逐行建筑阴影计算，避免每个格子遍历全部建筑；保留高度、孔洞、残片、破坏缺口和每 2.5 米沿途探索规则。
- 后台探索只合并迷雾，不覆盖移动后的 Token 坐标；切换地图、视觉来源、导入存档或重置迷雾时丢弃失效结果。
- 状态更新时复用同一共享角色的解析结果，保留每个 Token 和独立实例的移动能力判断，降低联机同步耗时。
- 透视继续只影响感知，实体光源和移动碰撞规则不变，存档与通信 schema 不变。
- 增加千米视野、425 米移动和放置延迟浏览器回归。后台代码增加最多 16 KiB 压缩 JS 预算，首屏、CSS、ZIP 及性能门槛保持不变。

联机主机和所有玩家请统一升级至 v2.5.1。
