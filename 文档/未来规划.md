# RPGmap 未来规划

当前开发基线：**v2.3.4**。

## v2.4.0 实施状态

本轮以 `dae58632844953c6f8edf061778e0d7f3d93d181` 为基线，在 `codex/v2.4.0` 开发；以下条目全部通过后才允许合并和发布。本节优先于下文旧版路线中的延期安排。

- [ ] Document 单一差量、离线增量 Collection、角色卡字段级刷新与草稿冲突。
- [ ] 资产上传/鉴权/迁移、模板库、资料库和 GM 权限编辑。
- [ ] World 4 原子升级、米制空间、四种移动方式和回合消耗。
- [ ] 球形感知、有限高度 LOS、光源及安全投影。
- [ ] 状态解释、Journal 和授权开门。
- [ ] 全量回归、七会话浏览器性能、Edge/Chrome packaged smoke。
- [ ] 由用户安排的真实 Radmin 验收；未通过前保持 Draft，不合并发布。
- [ ] v2.4.0 版本文档、main CI、正式 Release 与下载复核。

2026-09-05 本机初始基线：629 项测试全部通过；Node `24.14.1`，固定 2 Scene / 100 Actor / 500 Token / 1000 Feature State fixture，20 次预热、5 轮各 100 次；move/Feature/Scene 中位数分别为 `0.020906 / 0.021628 / 0.999614 ms`。首屏 JS gzip `11,713 B`，总 JS `269,252 B`，CSS `7,076 B`。GM + 6 Player、30 次预热和 200 次混合操作的网络确认 p95 为 `33.343 ms`；该数据仅为 WebSocket 收包，不代表 DOM、帧率或真实 VPN 验收。

已确认默认：模板静态继承不变，运行默认值仅用于新实例；移动预算未设置时不限，普通/困难地形/无专用速度游泳分别按 1/2/2 倍计算，适用倍率相乘，飞行不付地面及水域倍率。禁止用放宽测试、预算或本机测试冒充 VPN 实测完成发布。

### 第一批实现记录（尚未完成 v2.4.0）

- PR [#48](https://github.com/Xlking152/RPGmap/pull/48) 保持 Draft。`d235d89` 已接通单一 Audience-safe Document changes；普通提交不再在网络上同时携带等价 patch/changeSet，离线与 LAN 共用增量应用器。旧 schema 3 WAL 只在显式重放边界接受，不允许旧客户端协议降级。
- ActorSheet 已按 Part 依赖选择刷新，角色目录、Token、血条和当前摘要按 ID 更新并合并到帧。窗口草稿与输入节点独立保留；名称、分类、队伍及公开资料使用字段旧值校验，Ruleset 的可编辑运行字段同样检查旧值。并发冲突不会自动覆盖输入，可采用服务器值或显式重提。
- 新增默认关闭的本地性能诊断。网络确认、输入到下一帧、Document 应用、Sheet DOM 更新和帧耗时分别记录；诊断不进入 World、存档或 WebSocket，不将输入到下一帧冒充真实移动预览完成时间。
- 当前验证：654 项测试通过，374 个 JS/MJS 文件语法通过；npm 官方 registry 全依赖及生产 audit 均为 0 漏洞。构建、bundle、package verifier、Edge 与 Chrome packaged smoke 通过，包含输入节点/选区稳定、实例隔离、公开资料、LAN 投影、Fog 和 390px 布局。Chrome 使用官方 Chrome for Testing `152.0.7977.82` 便携版；未改动系统浏览器安装。
- 本轮 bundle：首屏 JS gzip `11,717 B`、总 JS `279,526 B`、CSS `7,076 B`。七个 WebSocket 会话混合操作 p95 `33.185 ms`；持续可见单 Token 请求/回应最大 `401 / 506 B`，测试固定要求各自不超过 `4096 B`。这不是七个浏览器会话的 DOM/Canvas/FPS 或真实 Radmin 验收。
- 截图保存在本地 `output/playwright/v2.4.0-input-edge/` 和 `output/playwright/v2.4.0-input-chrome/`，不打入运行包。尚未完成所有编辑入口的冲突验收、七浏览器性能、资产/正文存储、模板资料库、World 4 升级事务、米制移动、LOS/光源、状态解释、Journal 与授权开门；上方总清单因此仍未勾选。
- 应用暂保持 `2.3.4`、World `3`、Ruleset `1.0.0`、兰州地图 `1.0.6`；开发分支 Operation 已为 `4`。不得将此开发检查点包装为正式 `2.4.0`，不得合并 main 或创建 Release。

### 第二批实现记录（图片内容基础，不是完整资产资料库）

- `a1427d1` 接通不可变图片记录、按 World 隔离的离线 IndexedDB、LAN 鉴权 HTTP 上传/读取、哈希去重、引用查询及备份/WAL 删除保护。新头像与 XLSX 图片先存内容，再提交引用；替换头像通过窄字段 intent 和旧值校验，不回写旧整张 Actor。
- ZIP 导入/导出携带 World 和全部引用图片，严格校验依赖与解压边界，内容持久化失败保留旧 World。旧 JSON 与 data URL 仍兼容，尚未执行 World 4 升级迁移。
- 验证：669 项测试、382 个 JS/MJS 语法、全依赖/生产 audit（0 漏洞）、build/bundle/package verifier 与 Edge/Chrome packaged smoke 均通过。覆盖 HTTP 越权和伪造引用、IDB 配额/中断、备份保留、ZIP 错误、真实头像上传解码及运行状态不变。截图在 `output/playwright/v2.4.0-content-edge/` 与 `output/playwright/v2.4.0-content-chrome/`。
- 首屏 JS gzip `11,723 B`，总 JS `291,900 B`，CSS `7,076 B`，未调整预算。七 WebSocket 会话混合确认 p95 `42.952 ms`，单 Token 最大请求/回应仍为 `401 / 506 B`；这些数据不证明 DOM/Canvas、FPS 或 Radmin 达标。
- 尚缺正文存储、资产/模板资料库界面、依赖冲突重映射及完整升级事务；空间移动、LOS/光源、状态解释、Journal、开门和综合验收也未完成。PR 仍为 Draft，所有总体阶段保持未勾选，应用和数据版本未提前提升。

### 第三批实现记录（模板资料库，整体阶段仍未完成）

- `bb9879f` 增加 GM-only 资料库索引、不可变模板正文和图片依赖检查；正文按需加载，Player 快照和 Document changes 均不包含索引及正文。内容读取、删除、类别放置授权和新副本默认权限由服务器重新验证。
- 模板支持搜索、分类、标签、排序、收藏、归档、复制和存入资料库。复制与导入通过 Ruleset 重置消耗、清除授权，不改已有实例；静态定义与未知安全扩展保留。标签草稿跨目标保留，远端冲突不替换输入节点或选区。
- 独立模板 ZIP 携带全部图片依赖，预览前检查哈希，状态冲突重映射后用一个批次创建索引与 Actor。正文和图片先持久化；权限撤回、World 切换、损坏包及配额失败不写入新 Document。World ZIP 递归包含正文的图片依赖。
- 本批验证：688 项测试、388 个 JS/MJS 语法、npm 官方全依赖/生产 audit（0 漏洞）、构建、bundle、package verifier，以及 Edge/Chrome packaged smoke。覆盖模板 ZIP、图片去重、资料库越权、标签冲突、跨目标草稿、390px 详情控件和原有实例/地图/Fog 回归；成功截图在 `output/playwright/v2.4.0-library-edge/` 与 `output/playwright/v2.4.0-library-chrome/`。
- 固定 fixture 的 CPU move/Feature/Scene 中位数为 `0.021529 / 0.018352 / 0.899029 ms`（Node `24.14.1`）；七 WebSocket 会话确认 p95 为 `26.225 ms`，单 Token 请求/回应最大 `401 / 506 B`。这不是七前台浏览器 DOM/Canvas/FPS，也不是真实 Radmin 数据。
- 首屏 JS gzip `11,626 B`、总 JS `303,653 B`、CSS `7,076 B`，未调整任何预算。后续功能必须继续收敛体积，不得把现有预算余量视作完成性能验收。
- 仍缺 GM 权限界面全面收敛、旧 WAL 语义与内容共同备份的 World 4 升级事务、米制移动、LOS/光源、状态解释、Journal 和开门。前台浏览器性能及真实 Radmin 尚未验收；应用仍为 `2.3.4`，World `3`，PR 仍为 Draft，不合并、不发布。

RPGmap 已完成 World-first、Ruleset/Actor Contract、Scene Feature State、通用多人 operation 与发布链收敛。后续开发必须保持以下边界：Core 提供能力，Ruleset 解释规则，MapPackage 描述地图内容，World 保存 Campaign 权威数据，Scene 保存地图实例与 Token。

## 一、v2.3.4 已具备的基础

- World schema 3、operation schema 3、Status schema 4、Access schema 4、Infinite Horror Ruleset `1.0.0`。
- 启动顺序为 World → Ruleset → Active Scene → MapPackage → Runtime。
- Actor 属于 World 并作为模板；Token 属于 Scene。怪物/NPC/召唤物强制为 Unlinked，生命、伤势、状态、资源和当前形态只保存在各自 `actorDelta`。
- `Scene.featureStates` 独立保存每个 Scene 的门、机关、阻挡高度和扩展状态。
- Actor、Token、Scene、Status、Effect、Chat、Health、Combat、Feature 与 Fog 的普通联机写入使用轻量 Document Backend、服务器权威 patch/changeSet 和按 ID Collection，不发送整份 World。
- Local Server 使用基线快照加 checksum WAL，在 ACK 前刷盘；断线可按 revision/Audience 指纹续传最近 256 次或 5 分钟的安全 Document commit。
- Local/LAN 逐会话生成 AudienceProjection；隐藏 Token 不会进入玩家快照、补丁、Combat、Chat、Effect、Status、Access 目录或查询结果。
- 每个 Player 会话使用一个受控 Token 的圆形实时视野；圆心跟随 Token 最新权威坐标，精确/模糊范围的 5 米网格探索历史按 Scene 和队伍持久化共享，GM 可重置或局部重新隐藏。
- Infinite Horror 提供精确/模糊侦测范围、感官能力、亮度降级和实例覆盖；模糊敌影使用 Session opaque ID，只投影匿名轮廓和量化位置。
- Actor 权限区分 NONE/LIMITED/OBSERVER/OWNER，并与 Token visibility、实例控制和视野授权分离。
- Actor Sheet V3 使用显式权限上下文、Play/Edit 边界和多窗口；GM 公共档案与按 Token 实例裁剪的 LIMITED 状态摘要已成为正式运行链路。
- Token 普通拖放、Ctrl/Cmd 分段路径、WASD 与群组移动通过一次性 `token.movePath`，共用本地预测、权威回滚、权限、状态、逐段碰撞与 Combat Turn Lock 语义。
- Status V4 支持 Buff/Debuff/Neutral、快捷 HUD、白名单自定义定义和服务器权威 turns/rounds 持续时间。
- 兰州城通过 Built-in Registry 延迟加载；World Manager 首屏不静态加载 Leaflet、地图 CSS、地图逻辑、SVG 或数据资源。
- Windows 包使用严格白名单，只包含编译后的 `app/`、Local/LAN Server、空数据目录和操作指南。
- Candidate/Release 自动验证 audit、测试、500 Token/GM+6 Player 基准、tracked syntax、bundle budget、package manifest、ZIP 校验和、BAT、World Manager、逐用户权限投影、Fog Canvas、动态 Runtime、兰州 JSON/SVG、29 个 WebP，以及 Edge/Chrome 双浏览器 smoke。

## 二、下一优先级

### 1. External MapPackage 生命周期

- 定义可安装 manifest、资产目录、签名/校验和与安全限制。
- 支持 Registry discovery、安装、禁用、卸载和离线校验。
- 为 Scene pinning 建立 MapPackage 版本兼容、升级预览、迁移与回滚策略。
- 保持单一 Source of Truth；不得同时维护编译包与服务器 raw 地图的双写版本。

### 2. Server Multi-World Manager

- 服务器端 World catalog、创建、切换、归档和备份恢复。
- `/setup` 管理界面与权限隔离。
- 切换 World 时重新执行 World-first bootstrap，不在已加载 Runtime 中强行覆盖另一份 World。
- 明确数据根、上传资源和备份保留策略。

### 3. Asset Manager

- Actor/Token 图片、地图素材和用户上传文件使用稳定资产 ID，不继续扩大 data URL。
- 内容类型、大小、文件名、路径穿越和配额由服务器校验。
- World 只保存资产引用，删除资产前检查反向引用。

### 4. External Ruleset Package

- Ruleset manifest、版本依赖、安装/升级/隔离和兼容检查。
- 用不包含 Health、Forms 或 Infinite Horror 字段的第二 Ruleset 持续验证 Contract。
- 未知或不兼容 Ruleset 继续明确失败，不允许静默回退到 Infinite Horror。

## 三、Feature 与权限

当前 Feature State 修改保持 GM-only。Player 交互必须在服务器 operation 层增加权限，不得只隐藏前端按钮。

计划能力：

- `permissions.interactFeatures`，并细分 open、close、activate、use。
- locked、secret、GM-only 与探索状态。
- Server 按身份过滤秘密 Feature 数据，复用现有 AudienceProjection，禁止用 CSS 假隐藏。
- toggle、button、activate/deactivate/use 等通用控件。
- MapPackage 可声明 anchor、图标和展示策略，但不能注入自己的状态机或权限判断。

## 四、Elevation 与空间模型

当前只实现 Token 离地高度与 Feature 阻挡顶部高度。后续需要统一设计：

- ground elevation 与 absolute elevation。
- 建筑楼层、地下层、屋顶层和层间连接。
- 桥上/桥下多层 Navigation Surface。
- walk、fly、climb、swim 等 Movement Mode 与垂直移动成本。
- Token physical height、门洞净高和大型生物体积。
- 坠落、水深、弹坑深度、洪水水位。
- Vision、LOS、投射物与高度区间。

这些能力应扩展显式 `NavigationQuery`/mover context，不复制另一套路网或把规则写死进地图。

## 五、VTT 功能

在 Server、权限和资产边界稳定后逐步实现：

- 基于墙体、门、地形高度和光源的精确 LOS；扩展现有 Vision/Fog，不建立第二套探索存储。
- Secret Door、Trap 与 Exploration State。
- 更完整的 Initiative/Combat workflow。
- Dice、Check、Target 与聊天卡片。
- 更完整的规则包 Hook 与回合触发；不得把 Status V4 已有持续时间重新实现为平行系统。
- Scene Notes、Journal、手册与分享权限。
- 环境声音、地图动画与交互反馈。

Combat API 扩展、精确 LOS/Light、Asset Manager、通用 Hook 和第二 Ruleset 必须分别进入独立检查点，不与小型修复捆绑。

## 六、质量与发布

- 保留固定 fixture 性能基准；GM+6 Player 的 500 Token 同机回环综合 p95 设为 60 ms 发布门槛，真实 Radmin 验收另行记录 RTT、Direct/Relay 与浏览器版本。
- World Manager 首屏与总 bundle 继续使用确定性 gzip budget。
- 每个发布包必须从 tag commit 干净构建，`VERSION.json.commit` 与 tag 目标一致。
- Release 只包含 ZIP 与 SHA-256；raw `reference/` 继续留在源码仓库。
- 增加第二 MapPackage/Ruleset 后扩展浏览器 smoke，覆盖版本不兼容、跨包 Scene 与安装失败。
- 长期关注损坏存档恢复、未知扩展字段保留、幂等重放、断线重连和多个同源 Token 隔离。
- 增加大规模 Fog 网格压缩、历史裁剪和跨 Scene 性能基准；不得用客户端上报探索格换取性能。

## 七、不可回退约束

- 不恢复浏览器全局默认 Ruleset 覆盖已有 World。
- 不把 `preferences.entitySystem` 或 UI 投影变回持久化写入权威。
- 不让 Unlinked Token 修改 Base Actor。
- 不让 NPC/召唤物通过 `actorId` 运行操作或切回 Linked 模式。
- 不把 AudienceProjection、当前视野来源或 UI 选择写回权威 World。
- 不接受客户端提交的视野坐标或探索网格。
- 不在普通交互中恢复整份 `world.push`。
- 不把 `featureStates` 放回全局 preferences。
- 不让 Core 依赖兰州 Feature ID、分类或 Infinite Horror 私有字段。
- 不为地图、服务器模式或安装流程增加平行 Launcher/Runtime。
- 不删除、跳过或弱化迁移、权限、并发与发布测试来获得全绿。
