# RPGmap 未来规划

当前开发版本：**V1.4.3 Candidate**  
当前稳定 `main`：**V1.4.0**

状态标记：

- ✅ 已实现
- 🟨 部分实现
- ⬜ 计划中
- 🧭 Launcher 已预留入口
- ↩️ 已撤回方案

## 当前能力总览

| 子系统 | 状态 | 当前能力 |
| --- | --- | --- |
| MovementSystem | ✅ | Token 拖动、Waypoint、A*、吸附、路线预览、权限预检 |
| EntitySystem | ✅ | Actor / Token / Form、Runtime / Effects、XLSX 导入 |
| Selection / Measurement | ✅ | 单选、多选、框选、Ruler、角色测距 |
| Health / Damage / Healing | ✅ | SimpleHP / WoundTrack、伤害、恢复、生命条 |
| CombatSystem | ✅ | Combatant、Initiative、Round / Turn、GM 管理、Turn Lock |
| Chat / Game Log | ✅ | 聊天、系统 / 战斗记录 |
| Multiplayer WebSocket | ✅ | World Snapshot、Presence、revision / baseRevision |
| Player Identity | ✅ | pending → GM approve、Persistent User、Player Key |
| Actor Ownership | ✅ | NONE / OBSERVER / OWNER、Default Actor、多角色控制 |
| Portable Storage | ✅ | `app/ + world/ + maps/`，旧 `map/` / `data/` 迁移 |
| Internet Multiplayer | ✅ | Cloudflare Quick Tunnel、HTTP/2、Join Code / GM Secret |
| Web Launcher | ✅ V1.4.3 | Local/LAN/Internet、连接信息、日志、GM User 后台 |
| PowerShell Runtime | ✅ V1.4.3 | 单窗口承载 Node 并显示 Local/LAN/Public、Join Code、GM Secret 和日志 |
| `RPGmap.bat` Bootstrap | ✅ V1.4.3 | 唯一 Windows 入口，启动 PowerShell Runtime |
| Dynamic Launcher Port | ✅ V1.4.3 | 29999 冲突时自动回退或动态分配 |
| Native Windows Launcher EXE | ↩️ 撤回 | 实现并测试过，因实机诊断/兼容体验撤回 |
| World Manager | 🧭 预留 | Launcher 已预留窗口 |
| Scene Manager | 🧭 预留 | Launcher 已预留窗口 |
| Backup Center | 🧭 预留 | Launcher 已预留窗口 |
| Multi-Map / Scene | 🟨 | `maps/` 边界已建立，Registry / 切换待实现 |
| 可破坏地图对象 | 🟨 | 正式 Scene 数据模型待实现 |
| Fog / Vision / Target / Dice | ⬜ | 计划中 |
| Named Tunnel / 固定公网部署 | ⬜ | 计划中 |
| SQLite | ⬜ 暂缓 | JSON Store 当前仍足够 |

## 当前架构

```text
RPGmap.bat
   ↓
PowerShell Runtime
   ├─ Node.js Host
   ├─ Launcher / Server / Tunnel 输出
   └─ Local / LAN / Public / JoinCode / GMSecret
   ↓
Web Launcher / GM Control Center
   ├─ Server / Tunnel Lifecycle
   ├─ User / Ownership
   └─ Future Managers
      ├─ World Manager
      ├─ Scene Manager
      └─ Backup Center

Game Server :30000
├─ Web App
├─ World Store
├─ WebSocket
├─ Player Identity
├─ Actor Ownership
└─ Combat Turn Authority
```

长期原则：Server 是共享 World 最终裁决者；Web Launcher 负责交互管理；PowerShell 只做稳定 Runtime 和信息窗口；`app/`、`maps/`、`world/` 职责继续严格分离。

## 已完成阶段

### Movement / Entity / UI — ✅

Token Movement、Waypoint、A*、Selection、Ruler、Actor / Token / Form、XLSX 导入、AppShell 等基础能力已完成。

### Server / Realtime V1 — ✅

Node HTTP / WebSocket、静态 Client、World Snapshot、revision、Presence、冲突保护与 Server 权限验证已完成。

### Player Identity / Ownership — ✅

pending、approve、预创建 User、Player Key、NONE/OBSERVER/OWNER、Default Actor、多角色、Combat Turn Lock 与 Launcher Admin 已完成。

### Portable World / Map Boundary — ✅

```text
app/   = 程序
maps/  = 地图模板 / 资源
world/ = Campaign 实例状态
```

### Unified Launcher — ✅ V1.4.3

已经完成：

- `RPGmap.bat` 单入口；
- PowerShell Runtime 信息窗口；
- Web Launcher；
- Local/LAN/Internet；
- 动态 Launcher 端口；
- Join Code / GM Secret / Player Invite；
- Server / Tunnel logs；
- pending approve；
- User create/update/delete；
- Default Actor；
- NONE / OBSERVER / OWNER；
- Player Key rotation。

### Native EXE — ↩️ 已撤回

轻量 Windows EXE 曾实现并测试，但真实设备诊断成本高于收益，因此当前明确不采用。

## Launcher 已预留的未来模块

### 🧭 World Manager

- World 列表；
- 创建 / 复制 / 重命名 / 删除 / 归档；
- 启动前选择 Campaign；
- World manifest / schema / migration。

### 🧭 Scene Manager

- Map Registry；
- MapPackage 导入；
- Scene 创建 / 删除 / 排序；
- Scene 切换；
- reset / clone；
- 资源校验。

### 🧭 Backup Center

- 一键 Snapshot；
- 自动备份；
- 备份列表；
- 恢复；
- World 导入 / 导出；
- 升级前备份。

## 下一阶段核心

```text
World Manager
   ↓
Map Registry
   ↓
Scene Manager
   ↓
Scene Instance
   ↓
Destructible Object / Environment
```

随后再推进 Snapshot → operation / patch、Dice / Target / Fog / Vision、Named Tunnel / 固定公网部署。SQLite 只在 JSON Store 真正成为瓶颈后评估。
