# 1.1.0 成品校验记录

- 校验日期：2026-08-21
- 构建命令：`npm.cmd run build`
- 测试命令：`npm.cmd test`
- 自动化测试：67 项通过，0 项失败
- 最终 HTML 大小：3,989,034 字节
- 最终 HTML SHA-256：`325A5E8DC9C2FC3E9D496D97740BBD2D1364EB95F6A9E4D65393C079BC386F97`

## 已验证项目

- 城隍庙 Feature、院落 SVG 和固定标签存在。
- 城隍庙替代的 `market-shop-04` 不再存在。
- 地图成品不包含 A/B/C 史料覆盖标注。
- 所有建筑足迹正面积重叠为零。
- 非许可建筑与道路缓冲正面积碰撞为零。
- 两点测距文字显示在线段上方。
- 框选删除、清空玩家标记与场景事件保持隔离。
- 撤销直接删除最后一条场景记录，重置直接清空全部场景记录。
- 旧追加式场景存档可规范化导入。
- SaveV1 与地图版本 `1.0.0` 至 `1.0.4` 可迁移到 SaveV2 / `1.0.5`，非兼容版本会被拒绝。
- 范围几何或目标类别变化后旧破坏预览会立即失效，状态层也会拒绝过期预览。
- 导入文件和主要集合规模均有限制，迁移前自动存档会保留备份。
- 地表、黄河、瓦砾、既有地标和 18 个普通建筑变体共使用 29 项本地生成式 WebP，全部内联；单页不含 Canvas、外链素材或外部运行时资源请求。
- 50 栋剩余普通建筑全部且仅含一个生成素材节点，九组 18 个变体均被实际使用，节点全部位于对应 Feature 图组内。
- 离线 HTML 自带 `application-version=1.1.0` 元数据，可脱离工程文档核验应用版本。
- 55 栋建筑均具备用途、构造和说明；54 栋可进入并拥有足迹外入口，州衙仪门保持通道语义。
- 浏览与检查工具可直接打开建筑详情；内部角色名单、定位、恢复和进入操作状态正确。
- 角色头像上传经 Canvas 居中裁切为 `128×128` WebP；实测头像 Data URL 约 9 KB，地图与列表均保持固定圆形尺寸。
- EasyStar A* 使用 10 米网格；道路优先、普通地面补齐、建筑/河流/弹坑/积水绕行、城门通行、浮桥破坏和城墙缺口测试通过。
- 实测 1.85 公里路线生成 13 个折点；确认移动后最终坐标立即进入存档，视觉动画期间不会保存半途位置。
- 角色可进入建筑；建筑整毁后内部角色自动疏散到入口外最近安全格，撤销破坏不会自动送回。
- 显式弹坑几何冻结在 `damage.craterPolygon`；直接相交、12 米临河连接、超距、障碍阻断、1 米接触传播和撤销均通过测试。
- 临河 8 米验收场景生成 1 个入水沟、1 个坑内积水区和 1 条流线；首次破坏播放动画，导入与重放为静态终态。
- 生成建筑图层实际透明度为 0.62，普通建筑 SVG 屋体底衬为 0.24，台基填充不透明度为 0.78，阴影不透明度为 0.15；默认地图包仍不含 `<image>`、Data URL 或外部资源。
- 18 张普通建筑素材总量为 1,137,000 字节，单张最大 84,708 字节，均为最长边 768px 的 Alpha WebP；50 个实例全部使用 `xMidYMid meet`，不存在 `preserveAspectRatio="none"` 拉伸。
- 18 张素材逐张通过 RGBA、Alpha 包围盒、浅色背景和深色背景检查；无伪棋盘格、白边、彩边、暗底光晕或主体裁切。
- 九类普通建筑近景检查中，素材均完整落在对应足迹内；重复 SVG 瓦线、门、棚布、隔栏和工坊设施已在生成素材下隐藏，未出现双重细节。
- 建筑局部破坏使用稳定的 14 点深色不规则缺口，样板生成 3 根梁木与 2 片瓦片碎片，不再呈规则浅色圆斑。
- 州衙仪门、州衙廊房、营房、仓廪、马厩、工坊、市肆、普通民居和院落民居逐类通过局部破坏、整毁、恢复与撤销恢复测试。
- 州衙正堂、州衙后堂和城隍神祠各自的地标位图均位于对应可破坏 Feature 图组内，整毁与局部破坏不会残留脱离碰撞对象的位图。
- 四座州城门和金城关关楼的位图均位于对应可破坏 Feature 图组内；门洞与原道路方向一致，五个图组进入摧毁态后均正确隐藏。
- 四座州城角楼为独立墙体 Feature，与建筑足迹零重叠；角楼、浮桥、市易务正厅和货栈共七个新增图组的破坏态均正确隐藏。
- 黄河浮桥已替换为窄桥面、短船艏版本；总览仍可辨识，近景不再呈规则梳齿状，两端保持原道路落点，破坏态和离线资源状态正常。
- 总览、中景和近景三档可见标签均无屏幕空间重叠，地物检查轮廓位于标签层下方，不遮挡核心标签。
- 州衙正堂、北城墙中段、北门、城内树木和黄河浮桥均可通过真实地图点击准确选中，详情面板名称、类别、ID、状态和中心坐标正确。
- 已毁州衙正堂可直接恢复；随后撤销该恢复记录，州衙破坏重新出现。
- 场景记录正确显示“建筑 1、桥梁 1、城墙 1”，点击后定位并高亮 3 个关联对象；破坏预览列出 10 个可点击目标。
- 州衙破坏样板命中 17 个对象，10 个整毁建筑生成稳定瓦砾贴花，且贴花均裁切在对象足迹内。
- 顶部品牌与工具图标由本地 Lucide 按需内联，不依赖 Emoji 字形或外部图标请求。
- 1366 × 768 桌面工具栏保持单行；768px 与 390px 宽度下页面无正文横向溢出、按钮文字溢出或地图容器空带。
- 浏览器布局尺寸检查中，390px 视口的正文宽度和滚动宽度均为 390px，地图容器高度约为 487.5px；控制台 0 错误，离线 HTML 外部网络请求为 0。
- 最终运行包只含一份地图成品 HTML。
