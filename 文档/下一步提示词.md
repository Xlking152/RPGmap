# 下一步提示词

PR #13 完成后没有待续的 Ruleset V1 / World V2 迁移检查点。后续任务可直接使用下面的发布后复核提示词。

## 可直接复制的提示词

复核 RPGmap `v2.0.0` 发布状态。先 fetch `origin/main` 与 tags，完整阅读 `文档/v2.0.0-最终验收.md` 和 `文档/规则包迁移检查清单.md`，确认 PR #13 已使用 merge commit 合入 `main`，`main` 的 `candidate` / `windows-smoke` 全绿，annotated tag `v2.0.0` 精确指向实际 merge commit，标签发布工作流与 GitHub Release 成功，发布包及校验和存在。确认应用版本为 `2.0.0`，World schema 2、operation schema 1、`world.ruleset.id = "infinite-horror"` 与 Infinite Horror Ruleset `1.0.0` 均未变化。

只做只读发布复核，不重新实现已完成架构，不直接修改 `main`，不移动或重建已发布标签。若发现发布后问题，记录证据并从最新 `main` 创建新的修复分支与 PR；不要改写 PR #13 历史。
