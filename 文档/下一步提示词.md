# 下一步提示词

将下面整段复制到一个新任务中即可继续 PR #13。开始前确保仓库工作区指向 `refactor/ruleset-v1`，并允许任务读取 GitHub PR 与 Actions 状态。

## 可直接复制的提示词

继续处理 RPGmap PR #13，从 `refactor/ruleset-v1` 当前远端 HEAD 开始。先同步原分支并完整阅读 `文档/规则包迁移检查清单.md`，以其中记录的架构边界、兼容决定、已完成项和验收标准为准；不要重新实现已经完成的 CI 稳定化、Health Ruleset 或 Status Ruleset 迁移。

本轮实现下一个检查点“通用 Actor / Ruleset Contract”：为 Actor 建立稳定的通用外壳和 Ruleset 管理的 `system` 数据；扩展 Ruleset Contract，使其覆盖 Actor 默认数据、旧存档迁移、规范化、验证、派生、展示/Sheet 描述、属性路径和通用运行时操作；让 Infinite Horror 适配现有 health 与 B/L/A 存档而不丢失角色、Token、伤势、状态或自定义定义。Token、角色卡、聊天、导入器、Effect 与多人同步应只依赖通用 Actor/Ruleset 接口，并使用最小假 Ruleset 边界测试证明 Core 不会注入 Infinite Horror 语义。

保留 `world.ruleset.id = "infinite-horror"` 和现有版本号。代码文件、变量、接口与测试名称使用英文；中文仅用于界面文案和 `.md` 文档。允许修改代码、增加必要测试和文档。不要实现或提前混入 World-first 启动，除非它是让本检查点可测试所必需的最小兼容改动；World-first 仍作为后续独立检查点。

先做定向测试，再依次运行 `npm test`、JavaScript 语法检查、`npm run build`、`npm run package:local-server` 和 Windows packaged-server smoke。验证通过后形成语义清晰的独立提交，推送到原分支 `origin/refactor/ruleset-v1`，监控 GitHub Actions 的 candidate 与 windows-smoke，失败就继续修复到全绿。保持 PR #13 为 Draft，不得修改或合并 `main`，不得提升版本号。

完成后更新 `文档/规则包迁移检查清单.md`，记录真实 HEAD、提交、测试、Actions 链接、完成项和剩余事项；同时更新本提示词以便继续下一个检查点。清理临时日志、测试目录、构建产物和一次性探针，但保留可复用源码、测试及这两份 `.md` 交接文档。最后更新 PR #13 描述，并报告提交、测试、CI 和剩余事项。
