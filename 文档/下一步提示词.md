# 下一步提示词

将下面整段复制到一个新任务中即可完成 PR #13 的 Ready 前复核。开始前确保仓库工作区指向 `refactor/ruleset-v1`，并允许任务读取 GitHub PR 与 Actions 状态。

## 可直接复制的提示词

继续处理 RPGmap Draft PR #13，从 `origin/refactor/ruleset-v1` 当前远端 HEAD 开始。先 fetch、确认工作区干净，并完整阅读 `文档/规则包迁移检查清单.md`、本文件、`AGENTS.md`、`package.json` 和 GitHub Actions 工作流。以清单记录的最终架构边界、兼容决定和验证结果为准，不要重新实现 CI 稳定化、Health/Status Ruleset、通用 Actor Contract、World-first、多人 operation protocol、Ruleset 显式上下文、World 权威投影或 P2/P3 清理。

本轮只做“Ready 前最终复核”：确认远端 HEAD 与 PR HEAD 一致，PR 描述准确，最新 `candidate` 和 `windows-smoke` 全绿，PR 仍为 Draft；确认应用版本 `1.7.0`、World schema 2、`world.ruleset.id = "infinite-horror"` 和 operation schema 1 未变化。抽查普通交互不发送完整 World，完整 World 只用于初始化、认证快照、显式导入/恢复和 revision gap 恢复；抽查旧 SaveV1/V2 `characters` 与 `characterId` 只存在于迁移/兼容输入边界。

若远端没有新增代码，不再重复完整测试；核对最近一次记录的 62 项定向测试、381 项全量测试、236 个 JavaScript/MJS 语法检查、生产构建、打包和 Windows `/api/health` smoke 结果即可。若远端有新增代码，则按原顺序完整重跑验证且不得删除、跳过或弱化测试。

不要修改或合并 `main`，不要 force push，不要改变持久化格式。PR #13 保持 Draft，是否转 Ready 由负责人决定；除非负责人明确要求，不要自动切换状态。清理 `dist/`、`artifact/`、临时日志和一次性探针，最终报告远端 HEAD、Actions、Draft 状态、版本约束、兼容边界，以及是否具备转 Ready 的条件。
