# 下一步提示词

将下面整段复制到一个新任务中即可继续 PR #13。开始前确保仓库工作区指向 `refactor/ruleset-v1`，并允许任务读取 GitHub PR 与 Actions 状态。

## 可直接复制的提示词

继续处理 RPGmap Draft PR #13，从 `origin/refactor/ruleset-v1` 当前远端 HEAD 开始。先 fetch、确认工作区干净，并完整阅读 `文档/规则包迁移检查清单.md`、本文件、`AGENTS.md`、`package.json` 和 GitHub Actions 工作流。以清单记录的架构边界、兼容决定、World-first 与通用多人 operation protocol 完成状态、保留 P2/P3 和测试结果为准，不要重新实现 CI 稳定化、Health/Status Ruleset、通用 Actor Contract、World-first 启动或多人写入收敛。

本轮实施独立检查点“Ruleset 显式上下文与 Contract 收口”。先定位生产代码中的 `getActiveRuleset()`、`setActiveRuleset()` 和隐式 Ruleset 默认参数，重点检查 Actor model、XLSX importer、Health presentation、Status derive、Runtime 注册和导入/迁移调用链。Runtime 内部必须使用启动时固定的 `api.ruleset` 或显式传入的 Ruleset；不得因另一个 Runtime、测试或旧 bootstrap 选择改变全局 active Ruleset 而解释错误的 Actor/World。

全局 active Ruleset 只允许保留在启动选择、注册表查询和明确标注的外部兼容入口。Core 内部的 normalize、validate、derive、presentation、attribute path、operation 和 importer capability 调用必须具有显式 Ruleset 上下文。增加双 Runtime/双最小 Ruleset 回归测试，证明两个上下文交错调用时 Actor normalize、Status derive、Health presentation 和 importer capability 不会互相污染。

同步收口 Ruleset Contract，但不要重写整个系统：从通用 `RulesetV1.actor` 中移除未被 Core 消费的 `badStatusDefinitions` 专用字段，将 Infinite Horror 专用定义留在规则包内部或通用 presentation 描述中；为未知 Actor operation、未知属性路径、缺失可选 Health/Status/importer capability、未知 Ruleset 与不兼容版本规定稳定的返回值或错误码。调用方不得直接修改 Ruleset 私有 `actor.system` 数据，必须经过 attribute/operation Contract。

保持现有行为与兼容：World schema 2、`world.ruleset.id = "infinite-horror"`、应用版本 `1.7.0`、World-first 顺序、`world.operation` schema 1、operation patch/ACK/revision gap/幂等规则、旧 SaveV2、Actor.system、Synthetic Actor Delta、Health、Status、Effect、Scene 与 MapPackage 数据都不得改变。不得修改持久化格式；若必须改变且存在多个不兼容方案，立即暂停说明。

本检查点不要顺手处理其他保留项：未知扩展字段的 normalize/migrate 保留策略、本地 projection-to-World 兼容适配、Character 历史命名、旧 UI 文案和构建 chunk 警告。不要修改或合并 `main`，不要 force push，PR #13 始终保持 Draft。代码、接口、变量和测试名称使用英文；中文只用于界面与 Markdown 文档。

回归测试至少覆盖：最小 Ruleset、两个不同 Ruleset 上下文交错运行、旧 Infinite Horror Actor 与 SaveV2、无 Health、无 Status derive、无 XLSX importer、未知 operation、未知属性路径、Ruleset 私有数据不能绕过 Contract 写入、未知/不兼容 Ruleset，以及浏览器构建与 packaged server 仍注册同一规则包路径。测试应验证公共边界和最终数据，不只断言源码字符串。

先运行定向测试，再依次运行 `npm test`、仓库全部 JavaScript/MJS 的 `node --check`、`npm run build`、`npm run package:local-server` 和 Windows packaged-server smoke。代码与文档使用独立提交；推送到 `origin/refactor/ruleset-v1` 后监控 `candidate` 和 `windows-smoke` 至全绿。最后更新迁移清单、本提示词和 PR 描述，清理 `dist/`、`artifact/`、临时日志与一次性探针，并报告提交、测试、Actions、最终 HEAD、Draft 状态和剩余 P2/P3。
