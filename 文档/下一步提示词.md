# 下一步提示词

将下面整段复制到一个新任务中即可继续 PR #13。开始前确保仓库工作区指向 `refactor/ruleset-v1`，并允许任务读取 GitHub PR 与 Actions 状态。

## 可直接复制的提示词

继续处理 RPGmap PR #13，从 `refactor/ruleset-v1` 当前远端 HEAD 开始。先同步原分支并完整阅读 `文档/规则包迁移检查清单.md`，以其中记录的架构边界、兼容决定、已完成项和验收标准为准；不要重新实现已经完成的 CI 稳定化、Health Ruleset、Status Ruleset 或通用 Actor / Ruleset Contract 迁移。

本轮实现下一个独立检查点“World-first 启动”：把启动顺序改为先加载或创建 World，再从 `world.ruleset.id` 解析并激活 Ruleset，最后规范化和装载 Scene、Actor、Token 及其运行时系统。新 World 可以在创建边界使用当前默认规则包，但已有 World 不得被预先激活的 `infinite-horror` 静默覆盖；未知、缺失或不兼容 Ruleset 必须有明确、可测试的错误或兼容路径。旧 SaveV2/无 World 状态继续只通过现有显式迁移边界创建 World，并保留角色、Token、Synthetic Actor Delta、伤势、状态、自定义定义、Scene 与 MapPackage 引用。

让离线启动、World 投影和 Local/LAN 装载遵循同一 Ruleset 解析顺序，并增加冷启动、新建 World、已有 World、旧存档迁移、未知 Ruleset、Scene 切换和多人服务器装载的定向测试。不要重新展开 Actor Contract，也不要提前混入“通用多人操作协议/减少整份 World 推送”检查点，除非它是让 World-first 顺序可测试所必需的最小兼容改动。

保留 `world.ruleset.id = "infinite-horror"`、Ruleset/World schema 版本和应用版本 `1.7.0`。代码文件、变量、接口与测试名称使用英文；中文仅用于界面文案和 `.md` 文档。允许修改代码、增加必要测试和文档。

先做定向测试，再依次运行 `npm test`、JavaScript 语法检查、`npm run build`、`npm run package:local-server` 和 Windows packaged-server smoke。验证通过后形成语义清晰的独立提交，推送到原分支 `origin/refactor/ruleset-v1`，监控 GitHub Actions 的 candidate 与 windows-smoke，失败就继续修复到全绿。保持 PR #13 为 Draft，不得修改或合并 `main`，不得提升版本号。

完成后更新 `文档/规则包迁移检查清单.md`，记录真实 HEAD、提交、测试、Actions 链接、完成项和剩余事项；同时更新本提示词以便继续下一个检查点。清理临时日志、测试目录、构建产物和一次性探针，但保留可复用源码、测试及这两份 `.md` 交接文档。最后更新 PR #13 描述，并报告提交、测试、CI 和剩余事项。
