# 规则包迁移检查清单

## 文档用途

本文记录 PR #13（`refactor/ruleset-v1`）的规则包迁移边界、兼容决定、已完成检查点和下一阶段验收标准。后续任务应先阅读本文，再基于 PR 当前 HEAD 继续，避免重新分析完整历史。

## 架构边界

### Core

- 提供通用文档生命周期、权限、Actor/Token 定位、Synthetic Actor Delta 持久化、Effect 实例、状态层数、启用状态、属性变更和 capability 聚合。
- 只通过 Ruleset Contract 调用生命值、伤害、治疗、运行时操作、状态派生和展示逻辑。
- 不保存 Infinite Horror 专用状态 ID、B/L/A 字段名、伤势升级/治疗算法、默认伤害类型或死亡/昏迷阈值。

### Ruleset

- 持久化 ID 保持 `infinite-horror`。
- `RulesetV1.health` 负责 simple HP、wound-track、B/L/A 伤势升级与治疗、死亡/昏迷判断、格式化、默认伤害类型、运行时操作解释和展示结构。
- `RulesetV1.statuses` 负责内置状态定义和基于 Actor/context 的派生状态。
- 规则包提供角色卡、Token 血条和聊天表单需要的模式、类型、摘要、状态、条段与编辑字段。

### World

- 拥有活动 Ruleset 引用、Actors 和 `statusDefinitions`。
- 新建或迁移旧存档时，以活动 Ruleset 的内置定义为权威刷新内置状态，同时保留用户自定义状态。
- `World.statusDefinitions` 是客户端与 Local/LAN Server 共同读取的状态定义来源。

### Scene

- 拥有地图包引用和 Tokens，不拥有规则算法或 Actor 的权威业务数据。
- Scene 内的 Synthetic Actor 只保存相对基础 Actor 的 Delta。

### Actor

- 当前继续兼容已有 health runtime 与旧 B/L/A 存档形状。
- 下一检查点将引入通用 Actor 外壳和 `system`，把系统专用角色数据进一步收进 Ruleset 管理的命名空间。

### Token

- 通过 Actor 引用或 Synthetic Actor Delta 取得运行时数据。
- 血条、状态与交互能力由通用 Core 管线和活动 Ruleset 的 presentation/derive 输出驱动，不保存 Infinite Horror 算法。

## 本轮完成项

- [x] 在 Node 22.12.0 下复现并稳定多人服务器集成测试。
- [x] WebSocket 等待失败信息包含期望名称、最近消息与服务器错误；服务器清理等待子进程实际退出。
- [x] 将 simple HP、wound-track、B/L/A 升级/治疗、死亡/昏迷、格式化和默认伤害类型迁入 `src/rulesets/infinite-horror`。
- [x] `RulesetV1.health` 增加 `applyRuntimeOperation()` 与 `presentation`；Core 将 damage/healing 的 `type` 视为不透明值。
- [x] 统一伤势编辑入口为 `api.health.performActorOperation(actorId, operation)`；`set-wounds` 由 Infinite Horror Ruleset 解释。
- [x] 将内置与派生状态语义迁入 `statuses.definitions` 和 `statuses.derive()`。
- [x] World 创建/迁移时种子化并刷新内置状态定义，保留自定义定义。
- [x] Core Status 移除 Infinite Horror 状态 ID、名称、B/L/A 字段和 Health 模式判断。
- [x] Local/LAN Server 从 World 读取状态定义，禁止修改/删除内置定义，并剥离客户端伪造的 `builtIn` 标记。
- [x] 增加源码边界测试和最小假 Ruleset 测试，证明 Core 不会自动注入 Infinite Horror 语义。
- [x] 生产构建中发现并移除已退役的多人角色控制导入。
- [x] 完成本地全量测试、语法检查、生产构建、本地服务器打包与 Windows 打包服务器 smoke。
- [x] GitHub Actions 的 `candidate` 与 `windows-smoke` 全绿。

## 持久化与兼容决定

- `world.ruleset.id` 保持 `infinite-horror`，不更改现有存档身份。
- 继续读取现有 Actor health runtime 和旧 B/L/A 数据形状；迁移不得丢失角色、Token、伤势、状态或自定义状态定义。
- Ruleset 内置状态会按当前规则包版本刷新；自定义状态按 ID 保留。
- 服务器的状态权限与 capability 判定只依赖 World 中已持久化的状态定义和 Effect。基于生命值的派生状态由活动 Ruleset 在通用派生管线中生成；后续若需要服务器权威执行派生规则，应通过通用 Ruleset 运行边界实现，不能把 Infinite Horror 常量写回服务器 Core。
- 版本号保持 `1.7.0`，本检查点不提升版本。
- 除中文界面文案和 `.md` 文档外，新增代码文件、变量、接口与测试名称使用英文。

## 提交记录

代码检查点 HEAD：`8199dabfc09815b181e35854406cd7dd6241806e`

1. `572dd73` — `test: stabilize multiplayer server integration harness`
2. `f909156` — `refactor: move health semantics into infinite horror ruleset`
3. `302a39d` — `refactor: source status semantics from world ruleset`
4. `8199dab` — `fix: remove retired multiplayer character control import`

文档提交会在上述代码检查点 CI 通过后追加；PR 的最终 HEAD 以 GitHub PR 页面为准。

## 验证记录

本地验证日期：2026-08-28。

- Node 22.12.0：两个原失败多人服务器测试定向重复 3 轮，全部通过。
- Node 22.12.0：完整测试 342 项通过，0 失败。
- 当前本机 Node：`npm test`，342 项通过，0 失败。
- Node 22.12.0：136 个 JavaScript/MJS 文件语法检查通过。
- Node 22.12.0：`npm run build` 通过；仅保留既有 chunk size 警告。
- Node 22.12.0：`npm run package:local-server` 通过。
- Windows packaged-server smoke：`/api/health` 通过。
- 临时 `dist/`、`artifact/`、日志和一次性探针已在验证后清理。

GitHub Actions：

- [Build RPGmap Candidate #775](https://github.com/Xlking152/RPGmap/actions/runs/33101658324)：`candidate` 成功，`windows-smoke` 成功。

## 仍未完成项

- [ ] 通用 Actor 外壳与 Ruleset 管理的 `system` 数据。
- [ ] 完整 Actor/Ruleset Contract，包括角色数据规范化、默认值、验证、展示/Sheet 描述和可扩展运行时操作。
- [ ] 通用属性访问与 Effect 路径约定，避免 Core 依赖某一规则包字段。
- [ ] World-first 启动流程：先创建/加载 World，再按 `world.ruleset.id` 解析 Ruleset，随后装载 Scene/Actor/Token。
- [ ] 逐步收敛多人写入为通用操作协议，减少整份 World 推送。
- [ ] 最终全量回归、迁移说明和 PR #13 收尾；PR 在全部检查点完成前保持 Draft。

## 下一检查点：通用 Actor / Ruleset Contract 验收标准

- Actor 具有稳定的通用外壳；规则专用数据进入 `actor.system`，Core 不读取 Infinite Horror 专用字段。
- Ruleset Contract 明确定义 Actor 默认数据、旧存档迁移、规范化、验证、派生、展示/Sheet 描述和运行时操作入口。
- Infinite Horror 适配现有角色数据，旧 health/B/L/A 存档无损迁入或兼容映射到 `system`。
- Token、角色卡、聊天、导入器、状态 Effect 和多人同步只依赖通用 Actor/Ruleset 接口。
- 属性路径与 Effect 更新可以作用于通用 `system`，且最小假 Ruleset 无需 Infinite Horror 字段即可运行。
- 新增边界与兼容测试，覆盖普通 Actor、Synthetic Actor、旧存档、XLSX 导入、状态/生命值展示和多人权限。
- 依次通过定向测试、`npm test`、JavaScript 语法检查、`npm run build`、`npm run package:local-server` 和 Windows packaged-server smoke。
- 提交并推送到 `origin/refactor/ruleset-v1`，监控 GitHub Actions 至全绿；不得修改或合并 `main`，PR 保持 Draft。
